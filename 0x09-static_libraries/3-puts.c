#include "main.h"
/**
 * _puts - return a string
 * @str: string pointer
 * Return: Always 0
 */
void _puts(char *str)
{
	int i;

	for (i = 0; i[str] > '\0'; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
