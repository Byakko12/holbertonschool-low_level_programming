#include "main.h"
/**
 * print_rev - print string in reverse
 * @s: string pointer
 * Return: not return
 */
void print_rev(char *s)
{
	int i;

	for (i = 0; i[s] != '\0'; i++)
	{
	}
	for (i = i - 1; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
