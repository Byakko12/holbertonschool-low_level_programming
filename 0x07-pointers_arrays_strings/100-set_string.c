#include "main.h"
/**
 * set_string - sets the value of a pointer to a char
 * @s: double pointer pointed to char
 * @to: string pointed
 * Return: Not return
 */
void set_string(char **s, char *to)
{
	*s = to;
}
